version: '3'
services:
  myservice:
    environment: 
      - BOT_TOKEN 
      - DB_NAME 
      - DB_COllECTION_NAME 
      - DB_USERNAME 
      - DB_PASSWORD 
    build:
      context: ./
      dockerfile: Dockerfile
    image: bottom-bot/latest